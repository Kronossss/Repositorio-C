#include <stdio.h>

int main()
{
    int i,g,r,A[9999];
    g=0;
    printf("Digite um número, esse número também determinará quando a lista irá acabar :");
    scanf("%i",&A[0]);
    for(i=1; i!=9999; i++){
      printf("Digite o %iº numero da lista : ",i+1);
      scanf("%i",&A[i]);
      g+=1;
      if(A[0]==A[i]){
      i=9998;}
    }
    for(i=0; i<=g; i++){
        printf("%iº número digitado foi : %i \n",i+1,A[i]);
    }
    
    return(0);
}
